{
  "name": "vertox-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Vertox CRM - root wrapper so hosts that expect a root-level entry point (e.g. Railway without a custom Root Directory) can build and run the backend, which actually lives in backend/",
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=18"
  }
}
